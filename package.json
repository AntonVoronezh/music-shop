{
  "name": "music-shop",
  "scripts": {
    "start": "npx hardhat node",
    "cleanAndCompile": "npx hardhat clean && npx hardhat compile",
    "front": "npx create-next-app@latest front --typescript"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "hardhat": "^2.22.12",
    "prettier": "3.3.3"
  },
  "dependencies": {
    "mocha": "^10.7.3"
  }
}
